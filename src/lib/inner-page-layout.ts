/**
 * Top padding for the first content band under the fixed header on inner routes.
 * Home (`/`) uses its own landing layout; all other pages should use this for the hero/first section.
 */
export const INNER_PAGE_HERO_TOP_CLASS = "pt-24 md:pt-32";
