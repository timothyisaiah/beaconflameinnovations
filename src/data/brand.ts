/**
 * Brand image paths under `/public/assets` (PNG).
 * Replace placeholder files with final exports from design (see ImageMark + README).
 */
export const brandAssets = {
  logoMark: "/assets/logo-mark.png",
  logoHorizontalLight: "/assets/logo-horizontal-light.png",
  logoHorizontalDark: "/assets/logo-horizontal-dark.png",
} as const;
